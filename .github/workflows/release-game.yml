# Minimal placeholder release workflow for the game.
# Runs only when manually triggered (workflow_dispatch) so it won't run on pushes or send notifications.
name: Release Game - Placeholder

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  noop:
    name: No-op placeholder job
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder step
        run: echo "This is a placeholder release-game workflow. Replace with real release steps when ready."
